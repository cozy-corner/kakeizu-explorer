# Local dev defaults (non-secret, local Docker only). Loaded by `next dev`,
# not by production builds. Override via .env.local or platform env vars.
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=devpassword
